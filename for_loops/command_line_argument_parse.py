import argparse

parser = argparse.ArgumentParser(prog = 'Command_line_argument_parser.py',
                                 usage = 'Practicing Command Lines Arguments',
                                 description = 'What the program does',
                                 epilog = 'Text at the bottom of help' )

print(parser)
